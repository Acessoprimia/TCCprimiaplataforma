<link rel="stylesheet" href="../css/hdeslogado.css">
<link rel="stylesheet" href="../css/aparenciaFlutuante.css">
<link rel="stylesheet" href="../css/cookiebanner.css">
    

  <!DOCTYPE html>
<html lang="pt-BR">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <!-- Aplica o tema salvo (Configuracoes > Aparencia) antes da 1a pintura,
       pra pagina nao "piscar" clara e depois trocar pra escura. -->
  <script>
    (function () {
      try {
        var tema = localStorage.getItem("primia-tema");
        if (tema === "light" || tema === "dark") {
          document.documentElement.setAttribute("data-theme", tema);
        }
        var fonte = localStorage.getItem("primia-fonte");
        if (fonte === "grande" || fonte === "extra") {
          document.documentElement.setAttribute("data-fonte", fonte);
        }
      } catch (e) {}
    })();
  </script>
  <title></title>
  <link rel="icon" href="/favicon.ico" sizes="any">
  <link rel="icon" type="image/png" sizes="16x16" href="/favicon-16x16.png">
  <link rel="icon" type="image/png" sizes="32x32" href="/favicon-32x32.png">
  <link rel="apple-touch-icon" href="/apple-touch-icon.png">
  <%- include('./pwa.ejs') %>
  <link href="../css/theme.css" rel="stylesheet">
  <link rel="stylesheet" href="header-semantico.css">
</head>
<body>
  <a class="skip-link" href="#conteudo-principal">Pular para o conteudo</a>

  <!-- Overlay escuro -->
  <aside class="overlay" id="overlay" aria-hidden="true"></aside>

  <!-- Header -->
  <header class="site-header">
    <section class="header-inner">
      <!-- Logo -->
      <a href="/" class="logo">
        <img src="../image/logofooter.webp" alt="Logo Primia" />
      </a>

      <!-- Menu Desktop -->
      <nav class="nav-desktop" aria-label="Menu principal desktop">
        <ul>
          <li><a href="/telainicial">Início</a></li>
          <li><a href="/sobre">Sobre</a></li>
          <li><a href="/contato">Contato</a></li>
        </ul>
      </nav>

      <!-- Botão login desktop -->
      <span class="aparencia-header-slot">
        <button type="button" class="aparencia-trigger" id="aparenciaFlutuanteBtn" aria-haspopup="true" aria-expanded="false" aria-label="Aparência do site">
          <svg xmlns="http://www.w3.org/2000/svg" width="22" height="22" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
            <circle cx="12" cy="12" r="5"></circle>
            <path d="M12 1v2M12 21v2M4.2 4.2l1.4 1.4M18.4 18.4l1.4 1.4M1 12h2M21 12h2M4.2 19.8l1.4-1.4M18.4 5.6l1.4-1.4"></path>
          </svg>
        </button>
        <a href="/login" class="btn-login">Já tem uma conta?</a>
      </span>

      <!-- Botão hamburguer -->
      <button class="hamburger" id="hamburger" aria-label="Abrir menu" aria-expanded="false">
        <span></span>
        <span></span>
        <span></span>
      </button>
    </section>
  </header>

  <!-- Menu Mobile -->
  <nav class="nav-mobile" id="navMobile" aria-label="Menu principal mobile">
    <ul>
      <li><a href="/telainicial">Início</a></li>
      <li><a href="/sobre">Sobre</a></li>
      <li><a href="/contato">Contato</a></li>
      <li><button type="button" class="aparencia-trigger-mobile" id="aparenciaFlutuanteBtnMobile" aria-haspopup="true" aria-expanded="false">Aparência</button></li>
      <li><a href="/login" class="mobile-login-btn">Já tem uma conta?</a></li>
    </ul>
  </nav>

  <!-- JavaScript -->
  <script src="../js/hamburguer.js"></script>
  <script src="/js/envioUnico.js"></script>

  <%- include('./aparenciaFlutuante.ejs') %>

<!-- Alvo do skip link: primeiro ponto depois do cabecalho repetido -->
  <span id="conteudo-principal" tabindex="-1"></span>
</body>
</html>
